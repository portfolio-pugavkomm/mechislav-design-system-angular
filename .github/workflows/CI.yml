name: "CI pipline"

on:
  push:
    branches:
      - dev
      - master
      - release/**
  pull_request:
    branches:
      - dev
      - master

jobs:
  lint:
    uses: './.github/workflows/CI_lint.yml'
  test:
    uses: './.github/workflows/CI_test.yml'

